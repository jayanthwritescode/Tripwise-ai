# Local Environment Variables

# The project now uses Bring Your Own Key (BYOK) stored entirely in the browser's Local Storage.
# No build-time API keys or Firebase Environment Variables are required to run this application.
